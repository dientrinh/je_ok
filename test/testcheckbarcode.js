var assert = require('chai').assert;

describe('Test check generate barcode number', function() {
    this.timeout(15000);  
      it('should add numbers', function() {
    // add an assertion
	var numb=15;
	assert.equal(numb, 15);
  })
});

